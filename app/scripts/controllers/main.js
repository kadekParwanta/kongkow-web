'use strict';

/**
 * @ngdoc function
 * @name kongkowWebApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the kongkowWebApp
 */
angular.module('kongkowWebApp')
  .controller('MainCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
